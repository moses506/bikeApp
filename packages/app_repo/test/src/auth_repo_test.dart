// ignore_for_file: prefer_const_constructors

import 'package:test/test.dart';

void main() {
  group('AppRepo', () {
    test('can be instantiated', () {
      // expect(AppRepo.init(config:kDevConfig ), isNotNull);
    });
  });
}
